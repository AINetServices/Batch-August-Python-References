when did mcp server come out The Model Context Protocol (MCP) releasedd in november 2024 (used to connect third party apps, like connect to gmail, slack etc) it is open standard and open source (your favourite)

multimodal (input video images, tables) done using multimodal convert image, video etc to text and then evaluate it

autogen is a hobby thing (you can write a couple of lines, it is like the nodes in a round table (the nodes being AI agents))

Lanchain (more professional than autogen, connects the nodes (ai agents) 1-2-3-4 but like if at step 3 wrong then go back to 1)